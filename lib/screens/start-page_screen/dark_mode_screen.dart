import 'package:flutter/material.dart';

class DarkModeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(' DarkModeScreen'),
        backgroundColor: Colors.teal,
      ),
    );
  }
}
