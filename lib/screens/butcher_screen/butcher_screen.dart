import 'package:flutter/material.dart';

class ButcherScreen extends StatelessWidget {
  const ButcherScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
